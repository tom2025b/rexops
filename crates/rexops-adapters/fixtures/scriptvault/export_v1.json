{
  "_comment": "PROVISIONAL — envelope-shaped ScriptVault export sample for RexOps' scriptvault consumer. The contract (scriptvault.export.schema.json) fixes only the envelope; script item fields below are illustrative and will change when ScriptVault ships real versioned JSON.",
  "schema_version": 1,
  "source_tool": "scriptvault",
  "generated_at": "2026-06-04",
  "scripts": [
    {
      "id": "deploy-prod",
      "name": "deploy-prod.sh",
      "description": "Deploy to production with safety checks"
    },
    {
      "id": "backup-db",
      "name": "backup-db.sh",
      "description": "Nightly database backup"
    },
    {
      "id": "cleanup-logs",
      "name": "cleanup-logs.py",
      "description": "Rotate and compress old logs"
    }
  ],
  "favorites": ["deploy-prod"],
  "recents": ["deploy-prod", "backup-db"]
}
